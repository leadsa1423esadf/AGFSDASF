{
  "name": "counter-osint-telegram-bot",
  "version": "1.0.0",
  "description": "Telegram bot for managing OSINT database",
  "main": "netlify/functions/webhook.js",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step required'"
  },
  "dependencies": {},
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
